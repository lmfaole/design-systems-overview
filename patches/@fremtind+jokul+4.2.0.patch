diff --git a/node_modules/@fremtind/jokul/styles/core/jkl/_convert.scss b/node_modules/@fremtind/jokul/styles/core/jkl/_convert.scss
index 6c98e27..b947b24 100644
--- a/node_modules/@fremtind/jokul/styles/core/jkl/_convert.scss
+++ b/node_modules/@fremtind/jokul/styles/core/jkl/_convert.scss
@@ -20,6 +20,10 @@
     $result: 0;
     $digits: 0;
     $minus: string.slice($value, 1, 1) == "-";
+    $start-index: 1;
+    @if $minus {
+        $start-index: 2;
+    }
     $numbers: (
         "0": 0,
         "1": 1,
@@ -33,13 +37,18 @@
         "9": 9,
     );
 
-    @for $i from if($minus, 2, 1) through string.length($value) {
+    @for $i from $start-index through string.length($value) {
         $character: string.slice($value, $i, $i);
 
         @if not(list.index(map.keys($numbers), $character) or $character == ".")
         {
+            $signed-result: $result;
+            @if $minus {
+                $signed-result: -$result;
+            }
+
             @return to-length(
-                if($minus, -$result, $result),
+                $signed-result,
                 string.slice($value, $i)
             );
         }
@@ -54,7 +63,12 @@
         }
     }
 
-    @return if($minus, -$result, $result);
+    $signed-result: $result;
+    @if $minus {
+        $signed-result: -$result;
+    }
+
+    @return $signed-result;
 }
 
 /// Add `$unit` to `$value`
