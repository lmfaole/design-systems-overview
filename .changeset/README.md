This directory stores pending Changesets entries for the project.

- Add a new file here with `pnpm changeset` when a change should be reflected in the next version and changelog.
- Run `pnpm version-packages` when you want to consume pending changesets, bump the app version, and generate changelog updates.
