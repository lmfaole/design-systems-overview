---
"lmfaole": minor
---

Add Jøkul docs for Checkbox, Select, Text Input, and the shared icon set used by those components.

Document icon requirements explicitly in the component contract, and load the shared Jøkul webfonts stylesheet so Material Symbols render correctly in docs such as Checkbox.
